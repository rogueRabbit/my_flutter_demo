import 'package:flutter/material.dart';

class WaiMai extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('外卖'),
      ),
    );
  }
}